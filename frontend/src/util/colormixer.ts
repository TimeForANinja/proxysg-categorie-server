/*
import chroma from 'chroma-js';

// Scale the chroma color wheel and get 12 distinct colors.
const colorSpace = chroma.scale(['red', 'yellow', 'green', 'blue']).colors(12);

export const colors: Record<number, string> = Object.fromEntries(
    colorSpace.map((color, index) => [index + 1, color])
);
*/

export const colors: Record<number, string> = {
    1: '#00B8D9',
    2: '#0052CC',
    3: '#5243AA',
    4: '#FF5630',
    5:  '#FF8B00',
    6:  '#FFC400',
    7:  '#36B37E',
    8:  '#00875A',
    9:  '#253858',
    10: '#666666',
}
